import styled from '@emotion/styled';

const TopBorder = () => {
    return(
        <Border />
    );
};

export default TopBorder;

const Border = styled.div`
    background-color: #fff;
    border: 1px solid #fce287;
    border-radius: 20px;
    width : 70%;
    margin-left : auto;
    margin-right : auto;
`;